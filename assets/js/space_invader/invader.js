class Invader extends Entity {
    constructor(x, y, speed, spritePath) {
        super(x, y, speed, spritePath);
    }

    update() {
        
    }

    draw(ctx) {
        super.draw(ctx);
    }
}