class Bunker extends Entity {
    constructor(x, y, pv) {
        super(x, y, pv, "bunker-01");
        this.speed = 16;
    }

    update() {
        
    }

    draw(ctx) {
        super.draw(ctx);
    }
}